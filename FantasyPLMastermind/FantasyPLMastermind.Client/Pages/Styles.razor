

<style>
    body {
        background-color: #f2f2f2; /* Light gray background */
        font-family: 'Arial', sans-serif; /* Football-style font */
    }

    .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
        background-color: #fff; /* White container background */
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Soft shadow effect */
    }

    h3 {
        color: #2e3192; /* Dark blue for headers */
    }

    .manager-info {
        margin-top: 20px;
    }

    .manager-info p {
        margin: 5px 0;
        color: #555; /* Dark gray text color */
    }

    /* Football-themed button */
    .btn {
        background-color: #008cba; /* Blue button background */
        color: #fff; /* White text color */
        padding: 10px 20px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .btn:hover {
        background-color: #005f70; /* Darker blue on hover */
    }


    .arrow-up {
        font-size: 1.2rem;
        color: green; /* or red for arrow-down */
        position: relative;
        top: 1px; /* Adjust vertical alignment */
        outline: none;

    }

        .arrow-up:hover {
            color: darkgreen; /* Change hover color as desired */
        }

    .arrow-down {
        font-size: 1.2rem;
        color: red; /* Change color as desired */
        background-color: transparent;
        border: none;
        cursor: pointer;
    }

        .arrow-down:hover {
            color: darkred; /* Change hover color as desired */
        }

    /* Player Table */
    .player-table {
        width: 100%;
        border-collapse: collapse;
    }

    .player-table th, .player-table td {
        border: 1px solid #ddd;
        padding: 12px;
        text-align: left;
    }

    .player-table th {
        background-color: #f2f2f2;
        font-weight: bold;
    }

    .player-table tbody tr:nth-child(even) {
        background-color: #f9f9f9;
    }

    .player-table tbody tr:hover {
        background-color: #e2f3ff;
    }

   /* Formation here under v:   */

    .formation {
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .line {
        display: flex;
        justify-content: center;
        margin-bottom: 50px;
    }

    .player-card {
        margin: 5px;
        padding: 5px;
        border-radius: 5px;
        position: relative; /* Make player card a positioned container */
        color: black;
        text-align: center; /* Center-align content */
    }

    .player-image {
        position: absolute; /* Position the image relative to the player card */
        top: 0; /* Align the image to the top of the player card */
        left: 50%; /* Align the image horizontally centered */
        transform: translateX(-50%); /* Adjust the position horizontally */
        z-index: 1; /* Ensure the image is above the other content */
    }

        .player-image img {
            width: 80px; /* Adjust width of the image as needed */
            height: auto; /* Maintain aspect ratio */
        }

    .player-info {
        padding: 5px;
        background-color: #b8e994; /* Green background for player info */
        border-radius: 5px;
        margin-top: 80px; /* Add space for the image above */
    }



    .player-text {
        margin: 0;
    }

    /* Additional styling as needed */


    /* Additional styling as needed */


    /* Additional styling as needed */
    /* Home Page CSS under here */
    

    .search-container {
        width: 400px;
        padding: 20px;
        border: 1px solid #ccc;
        border-radius: 5px;
        box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
        background-color: #f9f9f9;
    }

    .search-input {
        width: 100%;
        padding: 10px;
        font-size: 16px;
        border: 1px solid #ccc;
        border-radius: 5px;
    }

    .search-button {
        width: 100%;
        padding: 10px;
        margin-top: 10px;
        background-color: #007bff; /* FPL blue */
        color: #fff;
        font-size: 16px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
    }

        .search-button:hover {
            background-color: #0056b3; /* Darker shade of FPL blue */
        }


</style>


@code {

}
